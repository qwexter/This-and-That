meta {
  name: Create record (with content)
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/records
  body: json
  auth: none
}

headers {
  X-User-Id: {{userId}}
}

body:json {
  {
    "title": "Meeting notes",
    "content": "Discussed Q3 roadmap. Next steps: finalize API design, review auth flow."
  }
}
